---
title: "Python Powered Terminal Tools"
description: "A curated list of terminal utilities you can install with uv."

date: "2026-05-06"
date-modified: "2026-06-06"

categories: [Dev Env]
image: ../../assets/images/posts/24-08-11-linux-in-windows/image.jpg

format-links: [html]
toc-depth: 0
---

Python-native terminal utilities that can be installed directly via `uv` or `pipx`.
Useful for either quick and dirty work, or a productivity boost in an environment where you don't have full privileges to install your favourite toolchain.

- **[`textual-code`](https://github.com/rishubil/textual-code)**

  The best modern TUI code editor. Built on Textual, offering VS Code/Sublime-like features (file explorer, multi-cursor, mouse support) out of the box.

  ```bash
  uv tool install textual-code --python 3.13
  ```

- **[`ranger-fm`](https://github.com/ranger/ranger)**

  The classic, highly customizable terminal file manager with Vim bindings and a multi-column preview layout.

  ```bash
  uv tool install ranger-fm --python 3.13
  ```

- **[`visidata`](https://github.com/saulpw/visidata)**

  The "Excel of the terminal." Incredibly fast tabular data explorer for CSV, Excel, SQLite, IPC and JSON files.

  ```bash
  uv tool install --with pyarrow visidata
  ```

- **[`glances`](https://github.com/nicolargo/glances)**

  The absolute powerhouse system monitor. Tracks CPU, RAM, Docker, GPUs, temperatures, and network speeds all at once. Works locally or as a web dashboard you can monitor from your network. Useful for checking status of local home server.

  ```bash
  uv tool install "glances[all]"
  ```

- **[`icdiff`](https://github.com/jeffkaufman/icdiff)**

  Improved colored diffs. Displays side-by-side terminal diffs with word-level color coding.

  ```bash
  uv tool install icdiff
  ```

- **[`hike`](https://github.com/davep/hike)**

  A terminal-based Markdown browser with clickable hyperlinks and beautiful rendering.

  ```bash
  uv tool install hike
  ```

- **[`rich-cli`](https://github.com/Textualize/rich-cli)**

  A massive upgrade to `cat`. Renders files with full syntax highlighting and proper formatting.

  ```bash
  uv tool install rich-cli
  ```

- **[`pygitzen`](https://github.com/SunnyTamang/pygitzen)**

  A Python-native Git dashboard to stage files, view diffs, and commit without remembering complex commands. Heavily inspired by LazyGit.

  ```bash
  uv tool install pygitzen
  ```

- **[`harlequin`](https://github.com/tconbeer/harlequin)**

  A drop-in terminal IDE replacement for DBeaver/DataGrip. Supports DuckDB, SQLite, PostgreSQL, etc., with a schema sidebar and query editor.

  ```bash
  uv tool install harlequin
  ```
